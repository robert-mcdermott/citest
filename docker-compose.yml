version: '2'

services:
    citest:
        build:
            context: .

        image: fredhutch/citest:dev

        ports:
          - "8000:80"

        container_name: citest-dev

        privileged: false
        restart: always
