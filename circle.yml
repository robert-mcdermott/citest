
machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t fredhutch/citest .

test:
  override:
    - docker run -d -p 8080:80 --name citest01 fredhutch/citest; sleep 10
    - docker exec citest01 /root/testsuite.py pass
