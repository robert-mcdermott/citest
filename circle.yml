
machine:
  services:
    - docker

dependencies:
  override:
    - sudo docker version
    - sudo docker build -t fredhutch/citest .

test:
  override:
    - sudo docker run -d -p 8888:80 --name citest01 fredhutch/citest; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://172.31.30.61:8888
    #- sudo docker exect -ti bash -c "/root/testsuite.py pass"
    

#deployment:
#  master: 
#    branch: master
#    commands:
#      - docker tag fredhutch/citest fredhutch/citest:production
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#      - docker push fredhutch/citest:production
#  develop:
#    branch: develop
#    commands:
#      - docker tag fredhutch/citest fredhutch/citest:develop
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#      - docker push fredhutch/citest:develop
